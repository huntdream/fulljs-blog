# This folder is for react client

# Tips

* Firefox will add a red border to required input field

  to disable it:

  ```
  input:required{
    box-shadow:none
  }
  ```

* setTimeout loop is not render blocking
  microtasks block rendering
